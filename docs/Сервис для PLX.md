10 мар. 2026 г.

# Система для управления учебными планами (файлами plx)

## Что есть сейчас (as-is):

Яндекс-диск, в котором находятся актуальные и архивные версии (утверждённых учебным отделом) plx-файлов, в разделении по некоторому критерию (кажется, по факультету).  
Архив устроен примитивно: внутри каждой папки факультета создана папка АРХИВ, куда складываются, видимо, предыдущие версии файлов (скорее всего, не более одной — более старые удаляются безвозвратно).

Процедура добавления нового УП предполагает:  
1\) удаление старой архивной версии  
2\) перемещение текущей версии в архив  
3\) добавление (загрузка) нового файла в качестве актуальной версии.

… всё вручную.

### Проблемы:

\- не видны "внутренности" файла plx для предпросмотра, поиска и сортировки по информационным полям  
\- не видна (вся) история изменений и ответственные за эти изменения  
\- негде оставлять пометки и комментарии к отдельным файлам УП (какой актуальный, какой изменённый и почему, какой на удаление, и т.п.)  
\- нет резервирования хранилища  
\- процессы подачи на рассмотрение (пересылки файлов), самого рассмотрения, обсуждения, исправления и принятия (добавления в актуальные) не формализованы и нуждаются в автоматизации.

## Что желательно сделать (to-be):

Информационная система (веб-сайт), доступная как внутри учебного отдела, так и на факультетах вуза, включая филиалы, с разделением прав доступа (читатели, разработчики УП, комментаторы/рецензенты, управляющие, админы)

Структура сайта в простейшем варианте должна содержать таблицу со всеми файлами, загруженными в систему, давать их скачивать оттуда и закачивать новые файлы (автоматически попадают в таблицу) или обновлять имеющиеся (сопоставление по совпадению имени).

| Файл | История версий | Действия | Факультет \[фильтр и сортировка\] | кафедра | направление подг. | год нач. подг. | … | Комментарии (редактир.) | статус/состояние (редакт.) | … |  |  |
| :---- | :---- | :---- | :---- | :---- | :---- | :---- | :---- | :---- | :---- | :---- | :---- | :---- |
| Ucheb\_plan\_09.03.04\_A\_RPIS\_O\_NOR\_FEVT\_POAS\_2023.plx\[скачать\] | дата1: Текущая версиядата2: скачать версиюдата3: скачать версию | \[скачать\]\[загрузить обновлённый\]\[удалить\] \[комментировать\]\[изменить статус\] |  |  |  |  |  |  | На проверке / отправлен на доработку / утверждён |  |  |  |
|  |  |  |  |  |  |  |  |  | Пример статусов рабочей программы (РП): ![][image1] |  |  |  |

 Это пример для 3-х учебных планов из программы РПД:  
![][image2]

Упомянутые файлы УП должны храниться в настраиваемом расположении (на выбор администратора сервера):  
\- на самом сервере в заданной папке;  
\- в сетевом расположении (FTP, WebDAV, ... — из поддерживаемого pyfilesystem2);  
\- в облачных хранилищах (Яндекс, гугл, ....?).

### Зеркала хранилища

Дополнительно могут подключаться хранилища-"зеркала", которые дублируют файлы основного хранилища.  
При этом второстепенные хранилища могут быть пассивными (исключительно бэкап туда) или активным, т.е. могут обновляться извне (как Яндекс-диск сейчас) — эти изменения надо отслеживать и отражать на основном хранилище, (и аналогично на других второстепенных) \-- этот механизм распространения изменений нужно продумать аккуратно, чтобы избежать зацикливания такого автоматического распространения изменений.  
Это будет полезно для резервирования и дополнительного удобства доступа к файлам, а также для возможности “бесшовного” переезда с одного хранилища на другое.

### Извлечение метаданных из файлов 

При каждом обновлении его контента файла метаданные должны извлекаться повторно на случай их изменения.  
Перечень полей предварительно известен и может расширяться.

Смысл прост: каждая версия файла обрабатывается на стороне сервера и снабжается метаданными, которые отображаются в основном интерфейсе сайта напротив самого файла в табличной форме, позволяют искать и сортировать таблицу по этим полям.

PLX \-\> модуль разбора файла.py :  
def extract\_plx\_metadata(path\_to\_file: str | Path) \-\> dict:  
“””  
Возвращает словарь с метаданными о файле и извлеченные из файла:  
	filename: str,  
	date: datetime,  \# Текущая дата.   
	extracted\_filelds: {  
	“факультет”: “Факультет электроники и …”,   
	“код\_кафедры”: “30”,   
	“имя\_кафедры”: “программное обеспечение …”,   
	“форма обучения”: “очная / заочная / …”,  
	“код направления”: “09.03.04”,  
	“год начала подготовки”: 2025,  
	“профиль программы подготовки”,  
	…  
}

“””  
…  
return {  
	‘filename’: ‘Ucheb\_plan\_09.04.01\_A\_TEK-2\_V\_NOR\_FEVT\_SAPR\_2022.plx’,  
	‘date’: datetime.now(),  
	‘extracted\_filelds’: {  
	“факультет”: “Факультет электроники и …”,   
	“код\_кафедры”: “30”,   
	“имя\_кафедры”: “программное обеспечение …”,   
	“форма обучения”: “очная / заочная / …”,  
	“код направления”: “09.03.04”,  
	“год начала подготовки”: 2025,  
	“профиль программы подготовки”,  
}  
}

### Управление дополнительными настраиваемыми полями

Концепция настраиваемых полей предполагает, что в основной таблице могут появляться сведения, которые изначально не предполагалось хранить, динамически, без изменения кода и вмешательства админов сервера.   
В панели управления сайтом должен быть раздел управления настраиваемыми полями. Будучи добавлено для объекта УП, доп. поле появляется для всех экземпляров УП в системе, и может получить своё специфическое значение для каждого экземпляра.  
Программно это не добавление колонки в основную таблицу объектов, а пара таблиц (сustom\_field)

Отображение извлечённых метаданных стоит делать через этот механизм, плюс должны быть доступны поля для свободного редактирования сотрудниками (политики, заметки, чат о файле)

### Хранение истории изменений.

Информация о файлах хранится в БД, включая все метаданные время обновления.

### Архивирование старых версий файлов.

все старые версии одного УП \-- в одном архиве (со сжатием)

### Выгрузка данных

желательна возможность выкачать всю таблицу с файлами и информации о них для дальнейшей обработки в Excel или другими способами.

Выгрузка в специальный HTML данных об образовательных программах (обсудим отдельно).

## TODO (Ставить плюсики по факту выполнения ↓)

\~ ознакомиться с этим документом (TODO) 
\~ реализовать разбор файла plx (Python, XML, JSON, возможно beautifulsoup4)  
- протестировать разбор на всех реальных файлах PLX
\~ отображение таблицы с данными о всех файлах (Python, Django, страница с шаблонизатором для вывода таблицы по данным полученного JSON)  
– чтение из облака (Python; “прикрутить” коннектор Yandex-disk к pyfilesystem2, научиться получать параметры подключения и подключиться на чтение и запись)  
– 

# Комментарии.

Материалы.

Ссылка на Я-диск с планами  
[https://disk.yandex.ru/d/D3HJ\_xoh3SddTX](https://disk.yandex.ru/d/D3HJ_xoh3SddTX)


